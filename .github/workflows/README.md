# CI

- **PR Android CI** — only check on open PRs and new commits to an open PR. One job, one universal signed APK artifact. Does not run after merge.
- **Flutter Release Builds** — manual only (Actions → Run workflow). Never on PR or push to main.
