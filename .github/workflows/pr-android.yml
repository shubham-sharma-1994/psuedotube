name: PR Android CI

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
      - master

permissions:
  contents: read
  actions: write
  checks: write

concurrency:
  group: pr-android-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  build:
    name: Build signed APK
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - run: flutter pub get

      - name: Decode keystore
        run: |
          set -euo pipefail
          if [ -n "${{ secrets.KEYSTORE_BASE64 }}" ]; then
            echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/noize.keystore
          elif [ -f android/dev-keystore.b64 ]; then
            tr -d '\n\r ' < android/dev-keystore.b64 | base64 -d > android/noize.keystore
          else
            echo "::error::No keystore"
            exit 1
          fi
          test -s android/noize.keystore

      - name: key.properties
        run: |
          set -euo pipefail
          SP='${{ secrets.KEY_STORE_PASSWORD }}'
          KP='${{ secrets.KEY_PASSWORD }}'
          KA='${{ secrets.KEY_ALIAS }}'
          [ -n "$SP" ] || SP='NoizeYtm2026!'
          [ -n "$KP" ] || KP='NoizeYtm2026!'
          [ -n "$KA" ] || KA='noize'
          {
            echo "storePassword=$SP"
            echo "keyPassword=$KP"
            echo "keyAlias=$KA"
            echo "storeFile=noize.keystore"
          } > android/key.properties

      - name: Build APK
        run: |
          flutter build apk --release \
            --build-number="${{ github.run_number }}" \
            --build-name="1.0.0+${{ github.run_number }}"

      - name: Checksum
        run: |
          set -euo pipefail
          OUT=build/app/outputs/flutter-apk
          ls -lah "$OUT"
          test -f "$OUT/app-release.apk"
          sha256sum "$OUT/app-release.apk" | tee "$OUT/app-release.apk.sha256"

      - uses: actions/upload-artifact@v4
        with:
          name: psuedotube-apk-pr${{ github.event.pull_request.number }}-run${{ github.run_number }}
          path: |
            build/app/outputs/flutter-apk/app-release.apk
            build/app/outputs/flutter-apk/app-release.apk.sha256
          if-no-files-found: error
          retention-days: 14
